import { BilibiliComment } from "../types";

// AI functionality has been disabled by user request.
// This file is kept as a stub to prevent import errors if referenced, 
// though it should not be used in the main application flow.
export const generateWinnerCommentary = async (winner: BilibiliComment, prizeName: string = "神秘大奖"): Promise<string> => {
  return "";
};
